version: "3.8"

services:
  mariadb:
    image: mariadb:10
    ports:
      - 3306:3306
    volumes:
      - ./db/conf.d:/etc/mysql/conf.d
      - ./db/data:/var/lib/mysql
      - ./db/initdb.d:/docker-entrypoint-initdb.d
    env_file: .env
    environment:
      TZ: Asia/Seoul
    networks:
      - backend
    restart: always
    
  app:
    container_name: arranginseats-app
    image: openjdk:17-alpine
    ports:
      - "8082:8080"
    volumes:
      - ./:/app
    working_dir: /app
    command: ["./gradlew", "bootrun"]
    depends_on:
      - mariadb
    restart: always

networks:
  backend:
